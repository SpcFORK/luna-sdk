// referral tests — structural (auth required)

referral := import('../referral')

fn run(t) {
	referral.stats().then(fn(result) {
		'referral: stats returns result' |> t.assert(result != ?)
		'referral: stats has ok field' |> t.assert(result.ok != ?)
	})
}
