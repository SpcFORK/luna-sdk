// luna-api/rewards provides reward pool information for Luna.

{ base: _base, bareReq: _bareReq, parse: _parse } := import('core')

// pool returns the current rewards pool info.
fn pool() _bareReq('GET', _base() + '/rewards/pool').then(_parse)
