// luna-api/collections provides collection fetching for Luna.

{ base: _base, bareReq: _bareReq, parse: _parse } := import('core')

// get fetches a collection by ID.
fn get(collectionId) {
	_parse(_bareReq('GET', _base() + '/collections/' + string(collectionId)))
}
